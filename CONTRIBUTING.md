# 贡献指南

感谢你关注劳动权益助手。Issue 和 Pull Request 请尽量聚焦一个问题，并说明影响的
客户端：HarmonyOS App、Python 桌面版或离线知识库工具。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 不提交 API Key、签名材料、用户数据、HAP 或构建缓存。
3. 修改 HarmonyOS 代码后运行 `./tools/harmony/build-debug.sh`。
4. 修改 Python 代码后运行相关测试，或：

   ```bash
   python -m unittest discover -s tests
   ```

5. UI 和设备能力变化请注明模拟器/真机、HarmonyOS 版本并附必要截图。
6. Pull Request 中说明变更、原因、验证方式和已知边界。

## 法律资料贡献

新增或更新法律资料时请提供：

- 官方文件名和发布机关；
- 官方来源链接；
- 发布、生效、修订或废止日期；
- 适用地区；
- 是否替代仓库中的旧版本。

不要仅凭模型输出修改法律条文。重新生成索引后，请同时更新构建报告和文档统计。

## 代码风格

- ArkTS 保持显式类型，避免绕过类型检查。
- 网络、文件和 JSON 操作必须处理失败状态。
- 模型引用必须经过本地引用 ID 校验。
- 新功能不得静默扩大数据上传范围。
- 优先拆分可测试的服务，避免继续扩大单一页面文件。

## 问题报告

请包含复现步骤、预期/实际结果、DevEco Studio 与 SDK 版本、设备型号和相关日志。
粘贴日志前请删除 API Key、材料正文和个人信息。
