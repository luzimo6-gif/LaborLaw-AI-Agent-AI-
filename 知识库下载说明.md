# 劳动法律智能体_根号三 — 知识库数据包

## 📦 这是什么？

这是劳动法律智能体的**可选知识库数据包**，包含中国劳动法相关的法律法规文档和已构建好的向量索引。

## ⚠️ 是否需要下载？

| 场景 | 需要知识库？ |
|------|------------|
| 只用 AI 对话（普法咨询） | ❌ 不需要 |
| 需要 AI 引用具体法条、案例 | ✅ **需要** |
| 需要完整的法律分析报告 | ✅ **需要** |

> 提示：即使不下载知识库，程序的基本 AI 对话功能仍然可用（基于 LLM 自身知识）。

## 📥 安装方法

1. 下载 `知识库数据包.zip`
2. 解压后得到以下文件：
   ```
   data/              ← 法律法规文档目录
   vectorstore.pkl    ← 已构建的向量索引
   ```
3. 将 `data/` 文件夹和 `vectorstore.pkl` 放到主程序所在目录（和 `.exe` 同级）

```
劳动法律智能体_根号三/
├── 劳动法律智能体_根号三.exe
├── data/                 ← 知识库数据放这里
│   ├── 法律/
│   ├── 行政法规/
│   ├── 地方法律 法规规章/
│   └── 规章及法律规范/
├── vectorstore.pkl       ← 向量索引放这里
└── ...
```

## 🔧 自行构建知识库

如果你有新的法律文件需要入库，可以：

1. 将 PDF/DOCX 文件放入 `data/` 对应分类目录
2. 运行主程序中的「知识库管理」→ 或在终端执行：
   ```bash
   python build_db.py
   ```
3. 等待建库完成（需要 API Key）

## 📊 数据概览

- 法律文件分类：国家法律、行政法规、地方法规规章、规章及法律规范
- 覆盖 34 个省级行政区的地方法规
- 支持 PDF 和 DOCX 格式
